name: C/C++ CI

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: g++ version
      run: g++ --version
    - name: cmake version
      run: cmake --version
    - name: build dir
      run: mkdir build && cd build
    - name: cmake
      run: cmake -DCMAKE_BUILD_TYPE=RELEASE ..
    - name: build examples
      run: cmake --build . --target thinks_poisson_disk_sampling_examples -- -j 6 -v
    - name: build test
      run: cmake --build . --target thinks_poisson_disk_sampling_test -- -j 6 -v
    - name: pwd
      run: pwd
    
